What happens when you initialize a repository?  Why do you need to do it?

 is generated an empty repository for the contents of the folder where we have executed the init.
 At this moment we can add files and create new commits to the existing repository.


How is the staging area different from the working directory and the repository? What value do you think it offers?

  the staging area is an intermediate area between the working directory a a commit. In this area we have those files we want to add to a commit


How can you use the staging area to make sure you have one commit per logical change?
  
  because you don't commit until you haven't added all the files referrede to a logical change

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

    Para qué o cuándo crearlas? Normalmente un repositorio va creciendo de manera lineal a medida que se van añadiendo funcionalidades en un proyecto, arreglando bugs, generando documentación. Las ramas, en cambio, son cuando se genera una ruptura o una bifurcación muy grande en el código como, por ejemplo, tener una nueva versión del codigo en desarrollo para experimentar, o porque se ha hecho la entrega de una fase del proyecto y se crea una rama para el desarrollo de la siguiente fase y se tiene otra para el mantenimiento de la que ya se ha finalizado.

